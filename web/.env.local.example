VERIFIER_API_URL=https://your-verifier-host
NEXT_PUBLIC_VERIFIER_URL=https://your-verifier-host
JWT_SECRET=your-secret-key-change-in-production

# Demo login (local/dev only - never in production)
# ALLOW_DEMO_LOGIN=dangerous_insecure_demo_mode
# DEMO_PASSWORD=min-16-characters-required

# Rate limiting: set TRUST_PROXY=true when behind a trusted reverse proxy
# TRUST_PROXY=true
